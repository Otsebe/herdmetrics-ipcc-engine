{
  "name": "herdmetrics-ipcc-engine",
  "version": "1.0.0",
  "description": "IPCC Tier 2 methane estimation engine for livestock — TypeScript",
  "main": "dist/calculations.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": ["ipcc", "methane", "livestock", "climate", "botswana", "typescript"],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
